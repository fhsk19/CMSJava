package com.hexaware.FTP105.model;
import java.util.Objects;
/**oStatus to show status of order
 * PLACED if order is placed
 * DELIVERED if order is delivered
 * CANCELLED if order is cancelled.
 */
public enum OStatus {
    /**
    * oStatus to show status of order
    * PLACED if order is placed.
    * DELIVERED if order is delivered
    * CANCELLED if order is cancelled.
    */
    PLACED, DELIVERED, CANCELLED;
}
